# 2026-04-05

## Discord Integration Debug & Fix

- `discord_listener.py` was broken: wrong CLI flags (`--to`, `--model`, `--channel`), `openclaw` not in PATH, and `engineer`/`scout` agents didn't exist yet.
- Fixed `discord_listener.py` to use correct `openclaw agent --agent <profile> --message <text>` syntax with full binary path.
- Created `scout` and `engineer` agents with separate workspaces (`workspace-scout/`, `workspace-engineer/`).
- **Critical finding:** Gemma-4-26b and Qwen3.5-9b behave as pure reasoning models on this LMStudio setup — output goes only to `reasoning_content`, `content` is empty. OpenClaw gateway can't parse the response and times out.
- Switched all agents to `qwen/qwen3-coder-30b` (primary) + `openai-codex/gpt-5.4` (fallback).
- Qwen3-coder-30b has ~45s cold-start after LMStudio idles it, then fast.
- Llama-3.3-70b OOM on moonstation (~61GB needed).

## Workspace Improvements (same session)

- Filled in `TOOLS.md`, `HEARTBEAT.md`, `MEMORY.md` for main workspace.
- Created proper `SOUL.md`, `USER.md`, `TOOLS.md` for scout and engineer workspaces.
- Fixed cron job delivery target from `@heartbeat` (invalid) to Philippe's Telegram ID `762062103`.
- Fixed Gemma-4 `reasoning` flag in openclaw.json to `true`.
- Cleaned up `discord_routing.json` (removed broken `linkedin_pulse` placeholder entry).
